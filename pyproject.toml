[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "automanga"
version = "1"
description = "Automanga"

[tool.setuptools]
packages = ["manga"]
script-files = ["automanga", "getmanga"]
